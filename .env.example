# Claude Code OAuth Token
# Get this from: https://claude.ai/settings/oauth-tokens
CLAUDE_CODE_OAUTH_TOKEN=your_token_here

# Assistant Configuration
# The name your assistant responds to
ASSISTANT_NAME=Kevin

# Signal Configuration (default messaging channel)
# Enable Signal messaging support
SIGNAL_ENABLED=true

# Signal bot phone number (international format with +)
# Example: +1234567890
SIGNAL_NUMBER=

# Signal API URL (default: http://localhost:8080)
SIGNAL_API_URL=http://localhost:8080

# Signal API Docker image (default: bbernhard/signal-cli-rest-api:latest)
# SIGNAL_IMAGE=bbernhard/signal-cli-rest-api:latest

# WhatsApp (optional — enable via /customize)
# To use WhatsApp instead of or alongside Signal, run the /customize skill.
# WhatsApp auth state is stored in store/auth/

# Container Configuration
# Container image name for agents
CONTAINER_IMAGE=nanoclaw-agent:latest

# Container timeout in milliseconds (default: 300000 = 5 minutes)
CONTAINER_TIMEOUT=300000

# Maximum container output size in bytes (default: 10485760 = 10MB)
CONTAINER_MAX_OUTPUT_SIZE=10485760

# Maximum concurrent containers (default: 5)
MAX_CONCURRENT_CONTAINERS=5

# Timezone for scheduled tasks (defaults to system timezone)
# TZ=America/New_York
